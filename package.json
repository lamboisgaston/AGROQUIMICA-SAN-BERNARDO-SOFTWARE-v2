{
  "name": "agroquimica-backend",
  "version": "1.0.0",
  "description": "Backend mínimo con Express + Prisma + SQLite",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
