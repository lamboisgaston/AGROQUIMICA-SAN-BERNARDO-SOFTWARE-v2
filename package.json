{
  "name": "agroquimica-backend",
  "version": "1.0.0",
  "description": "Backend mínimo con Express + Prisma + SQLite",
  "main": "server.js",
  "scripts": {
    "dev": "npx prisma generate && npx prisma migrate deploy && node server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "seed": "node prisma/seed.js",
    "start": "npx prisma generate && npx prisma migrate deploy && node server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "express": "^4.19.2",
    "pdfkit": "^0.18.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
